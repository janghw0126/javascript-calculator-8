# javascript-calculator-precourse


## 기능 요구 사항

입력한 문자열에서 숫자를 추출하여 더하는 **문자열 덧셈 계산기**를 구현한다.

- [x] 쉼표, 콜론을 구분자로 가지는 문자열을 전달하는 경우 구분자를 기준으로 분리한 각 숫자의 합을 반환한다.
- [x] 앞의 기본 구분자 외에 커스텀 구분자를 지정할 수 있다. 커스텀 구분자는 문자열 앞부분의 "//"와 "\n" 사이에 위치하는 문자를 커스텀 구분자로 사용한다.
- [x] 사용자가 잘못된 값을 입력할 경우 "[ERROR]"로 시작하는 메시지와 함께 Error를 발생시킨 후 애플리케이션은 종료되어야 한다.

---

## 과제 진행 요구 사항

- Node.js 22.19.0 버전에서 실행 가능해야 한다.
- 프로그램 실행의 시작점은 `App.js`의 `run()` 함수이다.
- 외부 라이브러리는 사용하지 않는다.
- 프로그램 종료 시 `process.exit()`를 호출하지 않는다.
- 파일, 패키지 등의 이름을 바꾸거나 이동하지 않는다.
- 자바스크립트 코드 컨벤션을 준수한다.
- `@woowacourse/mission-utils`에서 제공하는 `Console` API를 사용한다.
  - 사용자 입력: `Console.readLineAsync()`
  - 결과 출력: `Console.print()`

---

## 기능 구현 목록

- [x] 문자열 입력

- [x] 구분자 지정
    - [x] 쉼표, 콜론으로 분리
    - [x] 쉼표, 콜론 모두 사용 시에 분리
    - [x] 커스텀 구분자로 분리
    
- [x] 숫자를 추출하여 숫자의 합 출력

- [ ] 예외 처리하기
    - [x] 빈 문자열인 경우
    - [x] 지정된 구분자가 아닌 경우
    - [x] 입력받은 숫자가 양수가 아닌 경우
    - [x] 숫자가 아닌 문자를 입력받은 경우
    - [x] 커스텀 구분자를 잘못 사용한 경우
    - [x] 구분자가 2개 이상인 경우
    - [ ] 구분자로 끝나는 경우
    - [x] 숫자와 구분자 순서가 잘못된 경우

- [ ] 테스트
    - [ ] 빈 문자열인 경우
    - [ ] 구분자 없이 숫자만 입력한 경우
    - [ ] 지정된 구분자(쉼표)를 사용한 경우
    - [ ] 지정된 구분자(콜론)를 사용한 경우
    - [ ] 지정된 구분자(혼합)을 사용한 경우
    - [ ] 커스텀 구분자를 사용한 경우
    - [ ] 예외 처리

---

## 프로그래밍 흐름

**입력한 문자열에서 숫자를 추출해 더하는 계산기를 구현한다.**

1. 프로그램 실행 함수(run)에서 문자열을 입력받는다.

2. 입력값을 처리하기 위한 계산 함수(calculate)를 호출한다.

   2-1. 빈 문자열을 입력 받은 경우  
   - 0을 반환한다.
   
   2-2. 빈 문자열이 아닌 경우
   - 구분자로 분리한다.
   
      (1) 커스텀 구분자로 지정한 경우
      - //로 문자열이 시작된 경우 커스텀 구분자가 지정된 것으로 판단한다.
      - \n을 기준으로 나눠 구분자와 숫자 부분을 split()한다.
   
      (2) 기본 구분자 사용한 경우 (커스텀 구분자로 지정하지 않는 경우)
      - "," or ":" 을 기준으로 각 문자열을 split()한다.

3. 예외를 검증한다.
    - 지정된 구분자가 아닌 경우
    - 입력받은 숫자가 양수가 아닌 경우 o
    - 숫자가 아닌 문자를 입력받은 경우 o
    - 커스텀 구분자를 잘못 사용한 경우 (0)
    - 구분자가 2개 이상인 경우 (0)
    - 구분자 사이에 숫자가 없는 경우
    - 구분자로 끝나는 경우
    - 숫자와 구분자 순서가 잘못된 경우
    - 구분자만 입력한 경우

4. 검증이 완료된 숫자들을 모두 더한다.

5. 합산된 결과를 출력한다.